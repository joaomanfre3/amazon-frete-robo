# Copie para ".env" e preencha. O ".env" não é versionado.

# Pausa "humana" entre ações do robô (ms)
ACTION_DELAY_MS=700

# ─── Cérebro central (Neon Postgres) ───────────────────────────────────────
# Pegue em https://console.neon.tech → projeto frete-amazon-robo → Connection string
NEON_PROJECT_ID=
DATABASE_URL=
